HISTORICAL ONLY. Not an approved execution route. Never execute against a live account.
Embedded long image payloads omitted; original retained locally.
(()=>({text:document.body.innerText.includes('e편한세상 동탄역 어반원'),links:Array.from(document.querySelectorAll('a')).map(a=>({t:a.innerText,h:a.href})).filter(x=>x.h.includes('/85'))}))()
