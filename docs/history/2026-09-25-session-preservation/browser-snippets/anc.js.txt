HISTORICAL ONLY. Not an approved execution route. Never execute against a live account.
Embedded long image payloads omitted; original retained locally.
(()=>{let w=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),n;while(n=w.nextNode())if(n.nodeValue.includes('e편한세상 동탄역 어반원')){let a=n.parentElement;let out=[];for(let i=0;i<6&&a;i++,a=a.parentElement)out.push(a.tagName+'#'+a.id+'.'+a.className);return out}})()
