HISTORICAL ONLY. Not an approved execution route. Never execute against a live account.
Embedded long image payloads omitted; original retained locally.
(()=>{let es=[...document.querySelectorAll('*')].filter(x=>x.innerText?.includes('e편한세상 동탄역 어반원')); es.sort((a,b)=>a.outerHTML.length-b.outerHTML.length); return es.slice(0,5).map(x=>x.outerHTML.slice(0,2000))})()
