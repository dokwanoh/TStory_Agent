# Bounded immediate-publication MVP acceptance

Owner2026-09-21 approves the explicitly proposed three-part finish: duplicate-safe replay, one fact-only repair, and three sequential new-article combined-command live proofs. No new dashboard, notification service, generic recovery engine, recurring restart or reservation work.

## Fixed acceptance and authority

1. Same-ID replay: already saved means no second save; pre-save interruption preserves the claim and stops rather than blindly re-entering; unknown save result never retries. Existing receipt-only recovery may verify known saved identity without writing.
2. Quality: exactly one automatic text repair for facts-only failure; a second rejection or any other failed gate stops before publication. No manual body edits, image substitution, threshold change or new repair class.
3. Live proof: three distinct freshly researched articles, one sequential combined preparation/publication command per identity, four fresh contextual assets each, independent review and exact saved/anonymous verification. At most one existing bounded fact repair per article. No manual editor publication or manual text repair. No replacement identities after failure to manufacture three successes.

Identities: manual-20260921-mvp-proof-01, manual-20260921-mvp-proof-02, manual-20260921-mvp-proof-03. Owner approval covers these three immediate-public articles only, including scoped STOP suspension for each command and byte-for-byte restoration afterward. Automation3 stays PAUSED; reservations stay PAUSED. Per-run local grants expire at2026-09-21T08:30:00Z (operator safety deadline, not a promised schedule); content freshness can expire earlier. Protected authentication, actual access restrictions, uncertain save and failed gates remain stop conditions.

Do not expand scope after a failure. Diagnose only the observed defect and retain the same identity; maximum one evidence-based correction attempt per affected identity, no recursive retry. A quality rejection outside the approved fact repair ends that article's current attempt. Three proof successes are required before declaring this bounded MVP complete; fewer successes remain incomplete, not a percentage substitute.

## Verification mapping

- Existing test_immediate_execution valid/unknown cases verify one-save replay; real-Chrome test_immediate_runner_flow verifies one save, duplicate hold and read-only recovery.
- Add narrow pre-save interruption replay assertion if absent; no publisher behavior change unless it exposes a defect.
- Existing test_preparation_repair covers first repair success, second-review rejection and all non-fact gate exclusions.
- Live artifacts and actual CLI outputs are evidence only after observed. Fixture results do not certify public posts.

Observed gate1/2 result:31tests passed/11.16s (test_bounded_replay, test_immediate_execution, test_preparation_repair, real-Chrome intercepted test_immediate_runner_flow). New pre-save replay assertion passed against unchanged production code. Typecheck of the new test:zero errors/warnings. No duplicate-post recovery or quality algorithm was rewritten.

Preflight: isolated profile initially required login; normal Kakao login and saved origell selection returned MANAGER_READY15 with zero existing editors. No credential read or protected-auth bypass. Live proof01 launched after dry-run, shell syntax validation and automation3 PAUSED readback. JSON/bash LSPs absent; no installation, use existing parsers and bash -n instead.

One preflight defect fixed before article production: history_snapshot searched content/ only, omitting post97's promoted preparation package. Runtime inventory proved the omission; alternatives of missing package or sorting-only error were refuted by the existing package and discovery root. Added preparation/**/package/manifest.json discovery, retaining inspection exclusion and existing latest-five cap. Paths identify packages uniquely. Promoted local packages are conservative duplicate exclusions, not claims that those packages were published. Red1failed/1passed, green42passed/7.86s across history/flow/repair. Same real history query now includes97. No debugger, watcher or source instrumentation was introduced; temporary findings transferred here.

Official policy recheck2026-09-21: https://www.tistory.com/info/contract and https://developers.google.com/search/docs/essentials/spam-policies. Preserve copyright, platform access restrictions and original reader value; no bulk/spam or undocumented API. Per-article research/reviewer rechecks remain required. Legacy openApi page fetch returned an error and is not evidence of a supported API.

Regression after bounded history fix:684passed/174.61s including actual Chrome fixture tests; source and both new-test types zero errors/warnings; no-excuse3files, compilation and memory consistency passed. History collection retains one responsibility, typed JSON contracts and no logging/permission/retry changes. This result is not a live-publication count.

## Live attempt outcome

Proof01: research → selection → writing → four media → independent review completed. Title: 고용24 AI 해커톤 접수 시작, 아이디어만 준비하면 될까요? Review digest a1bce295936632c2808c085c39081f34ac2d3b0fae6c46127333ee47f2dcc9b0; approved=false. Failed checks: facts, reader_value, voice, rights, images, web_text_accessibility, policy. Freshness/originality/diversity/classification passed. The official guide supplied submission/development details the article treated as unknown; repetitive evidence-limit language weakened the answer. Reviewer also could not independently bind generated originals to final JPEGs, and links exposed long domain labels/list pages.

Actual CLI ended held/independent_review_held, exit2. Facts were NOT the only failed gate, so the existing fact-only repair correctly did not run. No approved package/publication-authority file was created, and original save journal has zero intents/receipts for this identity. STOP byte comparison passed; automation3 remained PAUSED. No new editor/post save. Original failed evidence retained under .artifacts/preparation/manual-20260921-mvp-proof-01/ and command log .artifacts/mvp-proof-20260921/run-01.log.

Proof02 and03 were not started: identical unresolved production/evidence risks would consume more work without establishing the agreed three-success gate. Result is0/3 public proofs, not completed MVP. No replacement identities, new repair class, weakened grader, manual rewrite or manual publish. The next bounded decision is whether to fix upstream factual evidence delivery plus original-to-JPEG provenance before a newly agreed live retest; this turn does not expand the one-fact-repair rule.

## Authorized post-fix retest

Owner subsequently requested actual public proof of the corrected combined command. On2026-09-21, invoke the previously unused proof02 under its existing unexpired grant, after commit d18ecef; do not reset proof01 or create a replacement identity. Proof03 is not invoked in this single-article retest. Command: `bash .artifacts/mvp-proof-20260921/run.sh 02`. Its EXIT trap restores the same STOP bytes; automation3 and reservations remain paused. Read-only preflight observed zero existing editors and MANAGER_READY in the dedicated profile. Dry-run listed all six stages with zero model calls and zero external writes.

The real command recorded research, selection, evidence and writing responses and reached media production. Selected topic is the still-unpublished 고용24 AI 해커톤 issue. The new evidence pack includes initial PDF/data-usage submissions, selected-team MVP development and dates; writing source URLs passed the checked catalogue parser. This is intermediate execution evidence, not a review, save or public-success claim. Final outcome is recorded below only after the command exits.

Final proof02 outcome: CLI exit2, held/independent_review_held, retry_safe=false. Title: 고용24 AI 해커톤, 기획서만 내면 되나요? Exact inspection digest9c6bb3a5bc22982fe7476d08fdc0cf6b72ee103b0559f7d0d69411ed2040d9b3. All four newly generated image originals were independently reproduced into exact final JPEG bytes; rights/images/diversity passed the independent reviewer, along with freshness/originality/reader_value/classification/policy (8 of11 checks). This does not mean8/11 of the project is complete.

Three review checks failed: facts and voice because the introduction says ‘시작된 접수를 앞두고’; web_text_accessibility because eligibility links to an empty FAQ and the full development/judging timeline links to a list showing only registration dates. Both facts have a supporting guide in the official-detail pack, but writing selected other catalogue URLs for those paragraphs. URL identity validation is not semantic claim-to-source binding. No unsupported root-cause assertion about model limits, browser failure or image upload is made.

The failure is not facts-only, so automatic text repair correctly did not run. No package/publication-authority was created; journal query for immediate/manual-20260921-mvp-proof-02 found zero save intents, receipts and attempts. STOP matches STOP.02.suspended byte-for-byte; automation3 remains PAUSED. No editor creation/upload/save, proof03 invocation, threshold change or replacement identity. Artifacts remain under .artifacts/preparation/manual-20260921-mvp-proof-02/ and .artifacts/mvp-proof-20260921/run-02.log. Result remains0/3 public proofs. The next bounded improvement would be claim-specific eligible source binding and coherent event-time wording before another expressly scoped attempt; no broader repair policy has been adopted.

Documentation/guard verification after retest: `PYTHONPATH=src .venv/bin/python -m tistory_growth_os.audit.memory` passed; `git diff --check` passed. Full existing regression command `PYTHONPATH=src:.venv/lib/python3.11/site-packages /opt/homebrew/opt/python@3.11/bin/python3.11 -m pytest -q tests browser_tests` produced698passed/174.40s. No runtime-source or test changes in this retest; these tests do not override the failed article review.
