# Bounded pre-media repair and fresh integrated proof

Decision PREP-004, owner approval2026-09-21: extend the executor, not a manual rewrite loop. One fresh immediate-public article is authorized under identity `manual-20260921-pre-media-proof-01`; preserve earlier proof failures. Daily operations and reservations stay PAUSED. The one-off wrapper restores original STOP bytes on exit.

## Acceptance and execution plan

1. Implemented: independent rejection can nominate only temporal/source-binding defects and exact affected blocks. Preserve title, taxonomy, tags, scenes/alts, all block identities/order and every unlisted passage. Only existing evidence may be used. Coverage/general voice/new-research failures are ineligible.
2. Verified locally: fresh independent text review after one repair; shared total repair budget means final review cannot trigger another correction. Immutable original/revised checkpoints and replay remain intact. Full722passed/181.97s, focused42passed/6.75s, source/scoped-test types clean, no-excuse8files and compile/schema checks pass. CLI help/dry-run, malformed run-ID hold and memory consistency are separate local checks, not public proof.
3. BLOCKED live proof: `bash .artifacts/pre-media-proof-20260921/run.sh` exited2/held/text_review_section_link_mismatch. Research→selection→evidence→writing→model text review completed; deterministic text validation rejected the model output. Media, final package and publisher did not run. Implementation commit71c13c0 was pushed before invocation. No manual substitution or second invocation.

Red evidence: new integration success/scope tests failed before implementation because text_review_held stopped immediately (2failed/2passed). Focused green42passed/6.75s; final full722passed/177.83s; memory/repair/diagnostic38passed/1.27s. These local passes are not publication proof.

Preflight: no existing editor; manager initially redirected to login. Normal Kakao saved-account selection restored access; a separate persistent-profile reopening passed MANAGER_READY. No credential extraction or protected authentication. Tistory official terms at https://www.tistory.com/info/contract were rechecked2026-09-21; the old /info/terms/ URL was unavailable. Pipeline independently checks current policy and source rights. Automation3 readback remained PAUSED. No excluded audits ran.

Tool limits: Biome/bash LSP unavailable, so JSON parser/bash -n were used without installation. uv absent; existing Python ran the dependency-free no-excuse checker. Personal editorial skill updated/read back with PREP-004; its generic validator cannot import missing PyYAML. No full skill-validator pass claimed.

## Exact live result and bounded diagnosis

Article: 고용24 AI 해커톤, 처음부터 완성된 서비스를 내야 하나요? Model text review returned approved=true, six checks=true, repair.scope=none and no issues. The deterministic validator correctly rejected a mismatch instead of trusting the approval flag.

Ten claim records across sections/1/paragraphs/0, sections/3/paragraphs/0, sections/3/paragraphs/1 and sections/5/paragraphs/0 listed guide+faq or guide+notice, whereas those actual sections select guide only. Every affected claim included guide. This proves surplus review URLs and the exact validator trigger; it does not independently certify all prose facts. The existing prompt says to attach fact URLs, but does not explicitly say not to copy every URL from an aggregate fact; this is a likely contributor, not a demonstrated prompt-only cure.

Recomputed subject matches0370931338efd05337d7bb00cbd6e0a5b3a5d060a04de57621fc601c5be75f5c. Review receipt matches17fa2488f29da8670126e3fe7f591d14d5567227270eb504474afcb4972a097f. Stale/mutated-subject hypothesis is refuted. Original writing/evidence/review remain immutable under `.artifacts/preparation/manual-20260921-pre-media-proof-01/`; run log/grant/STOP bytes remain `.artifacts/pre-media-proof-20260921/`.

No media.attempt, pre-media-repair/writing.attempt or publication-authority.json exists. STOP matches its suspended original; recurring automation3 remains PAUSED. No repair-output inference, publication receipt or external-write success is manufactured. The one-shot proof is not complete; old three-proof record remains0/3. The next bounded work is to align the review's per-block source selection with the executable contract, without weakening that contract, and independently re-evaluate it. Any further live invocation needs an explicit scope decision; do not reset this failed attempt or silently create a replacement.

Architecture self-review: new helper owns bounded correction scope, typed JSON is parsed at the boundary, no Any/suppressions/new logger or dependency. Existing orchestrator is207 pure lines (warning band, not over250); split by stage responsibility before adding more orchestration branches. Failed-review checkpoints are retained; no debug ports, source instrumentation or persistent debug processes were created.

## Boundaries

Reviewer contract now includes repair scope and affected block IDs. Approved reviews must request no repair. Refusal, malformed data, digest mismatch, self-review, out-of-scope changes and further rejection remain fail-closed. No threshold, freshness, source/rights rule or excluded audit lane changes. A prior manually corrected diagnostic is not used as this fresh article.
