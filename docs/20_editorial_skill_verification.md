# Reusable editorial skill verification

Owner asks for skill-first reuse of the three good final articles. Created `${USER_HOME}/.codex/skills/tistory-editorial-cycle/` with SKILL.md, two routed references, agents/openai.yaml and assets/run-record.json. No package/plugin installation, scheduler update, browser write or post mutation in this skill-creation increment.

## Evidence and actual cause limits

Main read all three reader-v4 full articles, reader-v3 VERIFICATION/attempt/visual-review and final readability README. Successful results were final private70/71/72 content/media/readback. Historical operations included corrected title selection, appended bodies and missed representatives, so the skill preserves final editorial form and detection rules, not a supposedly flawless click macro. Traffic/CTR/public reservation success is not established by these records.

Today's demonstrated process errors: input/readback mistaken for saved persistence; unsafe broad or unverified text selections; multiple recovery paths and editor tabs; no stable reusable run checklist. The underlying Safari/AX rendering defect and possible autosave collision remain hypotheses, not confirmed causes. A skill reduces decision drift; it does not itself fix a browser defect.

## Verification so far

JSON and YAML parsed with installed Ruby; allow_implicit_invocation=true checked. Standard skill quick_validate.py attempted with system and bundled Python; both unavailable because PyYAML is absent. No installation attempted. Equivalent frontmatter, naming, metadata and reference checks will be recorded after execution, not called the original validator's success.

Post-edit LSP hook could not inspect global skill paths outside repository cwd. This is a path-scope diagnostic limitation, not a reported syntax error; JSON/YAML are independently parsed. No runtime code or browser instrumentation changed.

Independent forward-test pending: nine cases covering false saved count, ambiguous save, private/public certification scope, taxonomy screenshots/disabled home topic, partial selection corruption, late-but-stale source, multiple editors, incomplete safe preparation, and non-transferable certification waiver. Reused existing independent review_gate_safety after fresh spawn hit fanout limit; no limit raised and no earlier verdict reused. No live write authorized by this rehearsal.

## Executed final results

Equivalent installed-Ruby validation PASS: YAML frontmatter and permitted keys, name/path and length, description, no unfinished scaffold, all three linked resources exist, UI prompt names the skill, short-description length, JSON template. This is not a successful run of the PyYAML-dependent validator.

Fresh independent review_gate_safety final result9/9 PASS. Read skill, two references, template, repository contract and reader-v4 examples. Cases1/5 require restoration rather than treating malformed content as saved;2 forbids another creation after uncertain save;3/9 prevent inheritance of private-result certification or per-article waivers;4 distinguishes labels from stored selection;6 retains freshness despite deadline exception;7 requires single-editor reconciliation;8 continues safe preparation through verified delivery. No behavioral blocker reported. Scope is a dry-run of agent decisions, not live UI, timing, upload, or remote write execution.

Reviewed SKILL.md SHA-256: eb9fdc93fa6e4196aa8308637f6f4f62a9bd72922c850d23abea7212581e88a9. The run-record template is observational, not an executable lock or schema validator. No autonomous-publishing reliability claim follows from this test.
# 2026-09-08 successful-path consolidation

Evidence: `.artifacts/readability-public-20260908.md` and STATUS minimal73save entry. Updated existing skill, not a second plugin/skill. New routed instructions prefer small basic-editor changes and branch on actual current visibility. Template now records operation, observed before-state/evidence, edit method, intended changes, preservation evidence and metadata preservation; absent facts default null/UNKNOWN. These are observational fields, not a runtime lock.

Manual instruction walkthrough (local, no simulated live PASS):

| Scenario | Decision now encoded |
| --- | --- |
| Memory says private, current editor/anonymous page says public | Preserve actual public state and dates; authorized edit only |
| One short visible paragraph needs a blank line | Small basic-editor edit, exact readback, scoped save and actual gap inspection |
| Existing gaps are already adequate | Preserve them; no accumulating blank paragraphs |
| Clipboard timeout or truncated body echo | Reconcile before another input/save; no blind repeat |
| Option-Down lands at next heading | Reject blind paragraph macro; fresh screenshot/target check |
| Generic fetch fails but ordinary anonymous browser opens article | Verify actual browser content; don't recreate or infer private state |
| Successful immediate public release | Record public evidence, not a certified reservation/scheduler |

Checks run:15memory/project tests passed2.83s; Ruby validates YAML name, all Markdown relative links and JSON neutral defaults. quick_validate.py remains unavailable because PyYAML is absent; external-path LSP cannot inspect the skill under this cwd. No installation/permission change. Exact SKILL hash423cc8312dc498f35e9ec672004256fddc7e96f0bae029cc5c38fe8fdc6790fe; route hash740f94d75b26c5ef79dc62e4ca80baeb337d83c5c1a6523cc2d22fd2490fd101. No new live write performed for validation.
