# Official-detail and media-derivation handoff

Owner2026-09-21 approves the two scoped upstream fixes after proof01 was rejected. This is local implementation/diagnostic authority, not a new publication identity, failed-checkpoint rewrite, extra repair class or scheduler resumption.

Acceptance: selected official detail contains answer/conditions/timeline with checked primary sources; unresolved central facts stop before writing. The same enriched evidence reaches writing and immutable final review. Generated media binds each final JPEG to a distinct fresh native PNG from the media session using exact independently repeated sips conversion, original/final hashes and preserved original bytes. Generation-tool presence alone is insufficient. No rights/semantic review threshold is reduced.

Completed scope: diagnosis, failing-first implementation, real local stage/asset QA and regression. The independent live-publication gate remains blocked and outside this local fix completion. No remote writes.

Diagnosis: research.json for proof01 included the official guide URL but omitted initial PDF/data-specification submissions and specific development dates from claim-level facts. The text-only writer received that incomplete candidate unchanged. Media inventory proved four native PNGs existed, but lacked a PNG-to-JPEG mapping. Inspection passed that incomplete inventory to the reviewer, so rejection was not merely a lost review attachment.

Red evidence: two handoff tests failed (writer/reviewer lacked the official detail; unknown central facts did not stop writing). Binding tests exposed missing executable original-to-final validation. Original failed run remains immutable.

Local runtime QA: evidence pass opened the actual official guide and produced initial PDF/data-specification requirements, post-selection MVP development and development dates. First writer included those facts but misspelled the long Punycode source twice; ordinary link validation correctly stopped it. This revealed an adjacent exact-source-transfer defect, not permission to normalize unknown domains. Added a per-request structured-output URL enum and request-hash binding; no new repair mode or manual URL edits. The failed diagnostic and original proof01 remain unchanged.

Actual historical native originals were exported ONLY into a separate local diagnostic, using the prescribed sips command. All four exact source/final derivations passed. This establishes byte lineage, not new-generation freshness, scene placement, semantic rights approval or live-publication success. Diagnostic path: .artifacts/evidence-handoff-20260921/. No new image generation or remote editor invocation.

URL-bound real writer diagnostic completed with exit0 and LOCAL_WRITING_VALIDATED. The unchanged title now has an immediate answer, initial PDF/data-specification requirements and the separate development window. parse_draft accepted all actual source links. First failed writing is preserved; the second diagnostic lives under url-bound-writing, not a replacement publication identity. No media generation, approved package, final all-check review or publication was claimed for this diagnostic. Existing full independent quality review still applies to the next authorized article.

Initial full regression:693passed,1failed/181.71s. The sole failure detected documentation views not yet regenerated from the new task row; views were regenerated and the memory CLI passed. Full final regression still required before completion.

Final regression:698passed/187.78s across tests and browser_tests. Scoped new/changed test typecheck and complete source typecheck:0errors/0warnings. No-excuse11files, compileall and git diff --check passed. CLI dry-run now lists evidence between selection and writing, model_calls=0/external_write_count=0; targeted dry-run red then green. STOP byte comparison unchanged, original proof01 save-intent count0. No debugger/listener/source instrumentation retained. The diagnostic scripts/artifacts are retained ignored local evidence, not publication packages or new operation grants.

Remaining boundary: these checks do not establish full all-check editorial approval, new-image production with the revised media prompt, or another public post. Future authorized integrated execution must pass the unchanged final reviewer and publisher checks. Old checkpoint hashes are not migrated or cleared to force reuse. No automated repair expansion.
