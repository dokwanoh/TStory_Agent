# TISTORY GROWTH OS — repository instructions

ADR-047 (owner2026-09-13): approved project-only Playwright installation and a separate automation browser profile. Do not copy personal profiles/cookies or change system permissions. Profile/runtime packages remain ignored, outside committed artifacts. This approves setup and local browser rehearsal, not live publisher cutover, session permanence, security bypass or any removed audit lane.

ADR-046 (owner2026-09-13): standing authorization to commit scoped changes, push feature branches and auto-merge reviewed PRs after exact-commit checks pass in dokwanoh/TStory_Agent; do not ask per change. Preserve secrets exclusions and branch protections. No force push, admin bypass, privacy conversion, plan purchase or gate waiver. Native auto-merge remains unavailable/not enabled in the observed private-repository configuration; record holds accurately rather than push directly to main. See docs/31_repository_delivery.md.

ADR-045 (owner2026-09-13): implementation starts against https://github.com/dokwanoh/TStory_Agent. Preserve remote history and exclude runtime/content/session data from Git by default. Final LLM selection targets the cheapest model that passes unchanged task evaluations; compare total accepted-article cost including retries, not token price alone. Do not silently escalate to paid models or relax gates. The new local save-intent journal is only a durable duplicate guard, not approval, a live publisher, scheduler cutover or proof of remote success. No new installation, paid API or profile authority is inferred.

ADR-044 (owner2026-09-13): a rejected/expired/duplicate research lead rejects that candidate, not the entire preparation slot. While the assigned release remains future and safe research is available, broaden sources, categories and queries and replace rejected leads without asking the owner again. Preserve five qualified drafts, all source/quality gates, same-slot identity and the existing deadline. Stop for genuine access/safety/ambiguous-save boundaries or expired target, not merely an unsuccessful first search. Do not fabricate candidates or weaken freshness to fill five places.

ADR-043 (owner2026-09-13): standing approval for the agent to upload this project's reviewed article media to nedamma.tistory.com through the normal Mac file picker in the existing authenticated editor, including when browser fileChooser.setFiles returns Not allowed. Do not ask again for this same upload route on authorized manual/recurring articles. Reconcile whether an upload succeeded before retrying; use fresh native UI and exact local asset paths. This authorizes the alternate normal picker route, not permission changes, security/login bypass, cookie extraction, unrelated files/destinations, platform-review evasion, or broader publishing scope. Stop on any new distinct access restriction.

ADR-042 (owner decision2026-09-12): remove WEB image pixel/display/lightbox verification from all operating-loop gates, including per-browser placeholder/render comparisons. EXCLUDED_BY_OWNER, not PASS; no repeated image-viewer checks or exception requests. Owner explicitly requests continuing same77 despite device-specific display difference. Preserve four intended assets/order/alt/representative identity and source/rights/fact/policy checks, text checks and exact native reservation/public-state readback. No re-upload/re-encoding/URL change to evade platform review; no override of an actual platform-denied publish action. Existing exclusions and schedules retained.

ADR-041 (owner decision2026-09-12): ALL print verification is permanently EXCLUDED_BY_OWNER throughout TISTORY GROWTH OS manual/recurring preparation, reservation, publication, recovery and completion. Includes print text, images, margins, clipping, pagination, print layout, PDF export/render/inspection for certification. Do not execute, block on, request exceptions for, or automatically restore any print lane. Overrides ADR040's retained print-text/layout requirements and all older print obligations. Historical evidence stays historical, never PASS. Preserve ordinary WEB text/images/alt/readability/contrast/keyboard, facts/24h/rights/policy, exact save/reservation/public checks and authority boundaries. Existing schedules unchanged.

ADR-040 (owner decision2026-09-12): print-image verification is EXCLUDED_BY_OWNER from all manual/recurring preparation, reservation, release, recovery and completion loops. Do not create/export/render PDFs or inspect print images solely to certify image appearance; no print-image gate, catch-up task, automatic restoration or repeated exception request. Historical failures remain history, never PASS. This narrowly supersedes earlier print-image obligations; retain ordinary web-image/four-image/alt checks, facts/freshness/rights/policy, text readability and non-image print layout requirements, exact save/reservation/public readback. Owner reporting a web image visible is evidence for that device, not proof another browser passed or permission to bypass platform review. No publication or broader gate waiver follows.

## Project operating rules

- Owner priority (2026-09-13, ADR-048): implement delivery/input/upload/reservation/readback first; single scheduled worker/recovery/alerts second; performance collection/refresh/learning third; low-cost LLM research/writing/review automation last (owner labels 1→3→4→2). Until generation automation exists, consume separately reviewed eligible packages; never reuse stale content or fill empty slots by weakening gates. This order does not authorize scheduler cutover, new data access or costs by itself.

- ADR-039 (owner decision2026-09-12): Lighthouse and third-party/external-ad investigation, auditing, scoring, remediation, monitoring and restoration are EXCLUDED_BY_OWNER from ALL TISTORY GROWTH OS manual/recurring preparation, publication, reservation, verification, recovery and completion loops. Do not run them, substitute another whole-site audit, create follow-up work, request exceptions, or block delivery because of missing scores or external-ad findings. No automatic restoration, including after skin/template changes; only a new explicit owner decision can restore them. This supersedes ADR-038, the Sep12 manual Lighthouse requirement and historical restoration notes. Preserve historical findings as history, never PASS. Retain our own article facts/freshness/rights/policy, four images/alts, headings/meaningful links, readability/contrast/keyboard/render/print and exact save/reservation/public readback checks. No ad hiding/settings changes, removal of commercial disclosures, global accessibility certification, or broader publishing authority.


- Scoped owner approval2026-09-12: current skin backup then administrator action-link/tag text color correction only. Preserve ads/layout/content/IDs and other gates; verify preview and deployed surface. Continue current fresh Chrome article afterward, no broader settings changes or quality waiver.

- 2026-09-12 scoped override: current fresh manual Chrome cycle must include actual Lighthouse, per owner “이번에는 lighthouse도 정상 시도”. ADR038 is not applicable to this cycle; no broader permanent restoration inferred. Native normal Chrome login required, no credential/session migration. Preserve other gates and ADR037.

- ADR-038 (owner2026-09-09): Lighthouse is temporarily OWNER_ACCEPTED_DEFERRED across this project's delivery loop until owner restores it. Not PASS and not permanent exclusion. No Chrome login or Lighthouse score prerequisite while deferred. Preserve all other current gates and ADR-037. Existing75 may proceed to future reservation after remaining checks; no duplicate or broader settings authority.

- ADR-037 (owner decision2026-09-09): actual screen-reader/VoiceOver/narration testing is excluded from ALL TISTORY GROWTH OS loop acceptance, readiness, publication, reservation, restoration and completion criteria. This is permanent unless the owner explicitly changes it, not a one-article deferral. Record EXCLUDED_BY_OWNER, never PASS; do not request activation, protected-display changes, local Mac access or later catch-up testing for this lane. This supersedes historical screen-reader obligations below and in older evidence. Preserve alt text, semantic headings, meaningful links, contrast, keyboard checks, print/rendering, facts/rights/policy and every other gate. No claim of full accessibility conformance follows. Initial MVP objective and original loop remain unchanged.

- Scoped print-only CSS approved2026-09-09: adjust print margins, image sizing and pagination, preserving screen appearance, advertising settings, all article content/media/IDs. No ad hiding, security change or quality-gate waiver. Use normal editor, backup and exact preview/saved-state verification; stop on ambiguous save.

- Scoped skin remediation approved2026-09-09: back up actual current skin source, then only source-link contrast, main/skip landmark, viewport zoom allowance, tag heading level and Korean wrapping. Preserve article content/identities/media, ads, accounts/billing/security. Verify scoped diff, home/list/article preview and saved anonymous surface before claiming deployment; stop on unavailable backup or ambiguous save. No broader redesign, publishing-gate waiver or protected-screen permission.

- Urgent74 exception consumed (2026-09-08): direct public delivery verified16:21KST, not reserved release. Resume ordinary detailed-print/Lighthouse requirements for subsequent articles; do not inherit this deferral. Screen-reader lane is subsequently excluded by ADR-037. Restoration evidence: `.artifacts/urgent-20260908/run.md`. Missing tools/settings still require explicit approval. Normal19:00schedule unchanged, not exempted.

- Preserve the owner’s initial boundary: `initial_execution_scope=discovery_to_offline_mvp`.
- `publish_mode=approval_required` is a state and quality boundary, not permission to send remote writes.
- Treat a local bundle, a loopback preview, and a dry-run as local work; keep `external_write_count=0`.
- Do not install packages, alter global configuration, relax permissions, initialize Git, or request secrets without explicit owner approval.
- Do not log, commit, fixture, or paste credentials, cookies, API keys, personally identifying data, or private analytics exports.
- Use public, current, primary policy sources for time-sensitive rules and record the checked date and recheck trigger.
- The retired Tistory Open API is not an implementation target. No undocumented endpoint or browser-write path belongs in the Offline MVP.
- Keep a human review boundary for ownership, claims of personal experience, high-risk content, monetization, privacy, and every external action.
- Facts require source/evidence and a checked date; label unverified operational reconstruction as `HYPOTHESIS`; label owner-dependent choices as `OWNER_DECISION_REQUIRED`.
- Do not represent unobserved analytics, costs, events, defects, or performance as zero. Use `UNKNOWN` and create an instrumentation requirement.
- Never portray unperformed work as a first-person experience, a test result, an endorsement, or an interview.
- Do not use keyword stuffing, content spinning, doorway pages, copied source text, hidden text, link spam, artificial traffic, or policy evasion.
- Original synthesis is required; quotations and licensed media must retain source and license provenance.
- Implement the Offline MVP with deterministic, local, typed contracts and structured audit output. Model and harness settings are adapters, not domain rules.
- Test a behavioral boundary before writing its implementation. Keep fixture-driven happy, blocked, malformed, replay, audit, and rendering scenarios reproducible.
- Fail closed: a missing evidence link, unverified owner experience, contract error, policy gate failure, or quality gate failure cannot enter the approval package.
- Preserve unresolved source-use review as `SourceEvidence.license_status=review_required`; it must block approval packaging. Other declared usage labels are not independent proof of reuse permission.
- A blocked `article-draft.json` is local inspection material, never an approval package. Keep its blocked status; do not transfer a Markdown review to a different generated representation without explicit review binding.
- `prepare-review` may export automatically eligible candidate bytes only inside an `inspection_only`, `approval_eligible=false` JSON envelope. It must not emit standalone approval artifacts or create a review receipt. Blocked inputs retain diagnostics only. The approval writer always regenerates from original validated input and separately checks the review (ADR-021).
- A local package must be idempotent. Retrying the same idempotency key must not create a second identity.
- Before creating or replaying an approval bundle, require a separate valid local review bound to every candidate artifact byte and the input/catalog identity. Recompute pipeline results at the writer boundary; reject changed stored files. Review, evidence and policy deadlines use the operational UTC clock and exclusive expiry. Local review records are trusted repository data, not authenticated human consent, source-use clearance or external-write permission (ADR-020).
- Treat a successful click or HTTP result as insufficient for any future external delivery; actual state and content must be rechecked after authorized delivery.
- Keep each module independently understandable, with no dependency on `.omo` at runtime.
- Prefer the standard library and checked-in contracts until a measured bottleneck and an approved installation justify more tooling.
- Keep Python public boundaries typed; do not introduce `Any`, `object`, type ignores, or unvalidated data past a boundary.
- Use `pytest`, compilation, type checking, dry-run, and manual rendered-surface inspection appropriate to each changed behavior.
- Record artifact paths and command results in `STATUS.md`; never convert a planned check into a success claim before it runs.
- Preserve unrelated files and other agents’ work. Do not revert unfamiliar changes.
- Ask owner interview questions one at a time. Continue safe local preparation between answers; do not treat an interview answer as permission for external actions.
- Select topics from audience demand and evidence-led opportunity research, not from an owner-expertise interview. The agent owns research, learning and explanation. Owner experience is optional; require evidence only if a specific personal-experience claim is actually used. Ask only for owner-only choices that cannot be researched or safely decided locally.
- Current editorial batch rule (ADR-024): select five distinct issue candidates whose occurrence or genuinely new announcement is within the trailing 24 hours at a recorded selection cutoff, and write a separate local draft for every selected candidate. Do not reduce the batch to one winner. A fresh crawl, reminder or future event alone is insufficient; record source time precision and exclude ambiguous boundary cases. Top5 is an evidence-qualified editorial ranking unless actual popularity data proves otherwise. This authorizes drafts only, not publishing or source-use clearance.

## Repository memory map

Successful assisted route memory (2026-09-08): before repeating article delivery, use `.artifacts/readability-public-20260908.md` and the skill's `references/private-revision-release.md`. For small readability changes prefer bounded basic-editor edits with full before/after readback over HTML replacement. Reconcile actual editor/public state before writes:70/71/72 were already public,73 was newly made public. Do not downgrade public articles based on stale memory or claim these runs certify unattended reservation. Preserve existing adequate spacing, source/media/IDs and scoped approvals.

Before new article production or delivery continuation, read `${USER_HOME}/.codex/skills/tistory-editorial-cycle/SKILL.md` and its relevant references. This owner-requested skill codifies reader-v4 editorial results and exact delivery checks; repository permissions remain authoritative. Skill creation/rehearsal is not live-publishing certification.

Available Tistory category and home-topic labels from owner screenshots are recorded in `docs/19_tistory_taxonomy.md`. Screenshots establish available choices only, never proof of this article's remote selection or save. Verify native selected values independently.

| File | Sole operational ownership |
| --- | --- |
| `PROJECT_SPEC.md` | Goals, non-goals, constraints, deliverables, definition of done |
| `PLAN.md` | Phase/milestone acceptance matrix and execution order |
| `STATUS.md` | Current evidence ledger, pending work, blockers, and next work |
| `DECISIONS.md` | Architecture decision records and rejected alternatives |
| `RISKS.md` | Risks, controls, evidence gaps, and recheck triggers |
| `METRICS.md` | Event definitions, source limits, quality checks, and unknown baselines |
| `BACKLOG.md` | Deferred work that cannot enter the Offline MVP |
| `OWNER_INPUT.md` | Bounded owner decisions and non-blocking interview questions |
| `docs/` | Evidence-based process redesign and implementation-facing detail |
| `contracts/` | Machine-readable schemas, policy evidence, decisions, and traceability |

## Delivery boundary

The first executable vertical slice is strictly local:

`TopicCandidate → SourceEvidence → ContentBrief → ArticleDraft → QualityReport → approval package`

It may produce editor-ready HTML, metadata, a manifest, rollback material, checksums, and audit data. It must not authenticate to Tistory, collect analytics, upload media, modify a post, or contact another service.

## Review gates

One-time urgent reservation rehearsal (2026-09-08 latest): owner approves one extra new article now, leaving19:00normal slot unchanged, and explicitly defers screen-reader/detailed-print/Lighthouse once more for this urgent identity only. Keep facts/rights/four-image/ordinary render/exact transfer/reservation and anonymous pre/post release checks. Record deferred not PASS; after success work on restoring all deferred checks. No standing daily-rate change or future waiver; no installs/global settings permission implied.

Scoped current-private batch release (2026-09-08 latest): owner explicitly requests applying readability changes to current private articles and making them public, plus updating the editorial skill. This supersedes private-only authority for the identified current editorial batch, not all historical private posts. Reconcile exact IDs before each write; current known editorial candidates70/71/72/73, earlier Gemini scope must be resolved from inventory. Preserve facts/media/sources/identities. Public permission is granted, but failed representation, policy, source or freshness checks still hold the affected article. No blanket gate waiver, new post, global settings or runtime-publisher authority.

Scoped drone private inspection (2026-09-08): owner explicitly chooses no category for this article and approves saving current drone draft privately to inspect its saved rendering instead of requiring the broken preview before private save. Keep same20260908-1200-ttukseom-drone identity, exact reviewed prose/four images, source/rights checks, duplicate check and one-shot save. This is not a visual PASS or public-release approval in this step. Keep private while inspecting; never recreate after uncertain save. Other article/scheduler gates are unchanged.

Scoped delayed-delivery exception (2026-09-08): owner explicitly permits the single noon drone article to be delivered after12:00 if necessary. This lifts only that article's noon deadline, not facts/freshness/media/quality checks, duplicate prevention, classification or saved-state verification. No automatic deadline waiver for future slots.

Noon exception transfer (2026-09-08, ADR-036): owner confirms moving the unused one-time screen-reader/detailed-print/Lighthouse deferral to ONE new article for today's12:00 slot. Morning AirPremia remains unreserved and ineligible for noon due to freshness. This is a transfer, not a second exception or recurring waiver. Research/select one new qualifying issue; retain all other review/media/source/policy/taxonomy/duplicate/reservation gates. Target remains future12:00Asia/Seoul; no automatic rescheduling if missed.

One-time morning certification exception (2026-09-08, ADR-035): owner says “이번만제외” to excluding screen-reader, detailed-print and Lighthouse certification from reservation prerequisites. Applies ONLY to the current September8 08:00 AirPremia article identity20260908-0800-airpremia-chusk20 and its reviewed draft/four media. Record those lanes OWNER_ACCEPTED_DEFERRED, never PASS. Retain source/rights/freshness, factual and image integrity, exact editor representation, ordinary visual/accessibility checks, taxonomy, duplicate protection, future reservation time, recovery stop rules and saved-state confirmation. No change to subsequent articles or recurring certification policy; no installation, global settings, historical-post edit or unrelated waiver.

Continuous delivery completion (2026-09-08, ADR-034): the owner explicitly requests creation through native reservation in one continuous run. For an authorized preparation run or manual slot article, continue research → draft/four images → required reviews → editor entry → existing category/home-topic selection → native reservation → saved-state readback without a routine local-draft handoff or per-post permission question. Local completion alone is not delivery completion. Actively execute pending safe checks; stop only for an actual failed gate, unavailable prerequisite, expired target or authority/access boundary, with concrete evidence and preserved artifacts. This does not waive any quality/policy/accessibility/recovery gate, authorize existing-post repair, or turn a saved reservation into confirmed public release. Resume the same date/slot/content identity; never create a duplicate. ADR-033's one-article scope remains, but its local-only stopping interpretation is superseded.

Manual single-article scope (2026-09-08, ADR-033): owner asks to produce one article now for the upcoming08:00 slot. Complete that one local draft/media deliverable without making it depend on four additional finished drafts; the recurring ADR-024 batch rule otherwise remains. Standing conditional reservation authority does not waive exact-representation, public-accessibility or recovery gates. Do not turn local production success into a reservation claim.

Advance native reservation and taxonomy delegation (2026-09-08, ADR-032): supersedes ADR-031's immediate-publish timing only. Start preparation at05:00/09:00/16:00Asia/Seoul and use Tistory's normal editor to reserve for08:00/12:00/19:00 on the same date, respectively. Select each new article's existing blog category and one currently available home topic by actual subject/search intent; record exact selected labels/IDs where visible and rationale, and read them back with the reservation. No category-tree creation/renaming or historical reclassification. No suitable or verifiable option means hold, not misleading classification. Freshness must pass both selection time and scheduled PUBLICATION time: a source event must already have occurred and must be strictly less than24hours old at release; ambiguous boundary timestamps are excluded. Verify reserved date/time/timezone/status/identity/title/body/four images/taxonomy immediately after save; record scheduled_verified, not published_verified. At08:00/12:00/19:00 the same heartbeat performs read-only release checks, never another creation. Failed/missing gates prevent reservation; a late/missed slot must not become immediate publication or catch-up. Existing immutable package gates remain. Unknown remote state blocks retry. Pausing the heartbeat does not cancel a server-side reservation: report known reservation IDs/times and request separate authority if cancellation or existing-post repair is needed. Actual normal-editor reservation/release E2E remains unverified until exercised; old private-only approvals do not certify it.

Conditional recurring public-delivery authority (2026-09-08, ADR-031): owner explicitly approves public automatic posting without per-article confirmation provided trailing-24-hour issues and current reader-v4 prose/image standards are retained and failed verification prevents posting. Automation3 may run at08:00/12:00/19:00Asia/Seoul, one new article per slot, maximum three per day. This is a scoped exception to per-action human confirmation, not a waiver of quality, policy, review-binding or route-readiness gates, and does not add network writes to the offline Python runtime. Use only the approved normal editor and owner's existing session after current-policy and route checks. Each article needs a square representative cover plus three inline images, evidence/rights checks, pre-write manifest/snapshot, exact title/body/media checks, duplicate protection and saved/public-state readback. Missing or failed checks, expired login, unknown save state or concurrent execution stop the slot; no blind retry or catch-up burst. Existing private posts70/71/72 remain private. No existing-post mutation/deletion, ads/billing/skin/account changes, new installation or paid-service expansion. Preserve ADR-024's five-candidate local drafts; recheck freshness immediately before publication. Pause automation on policy/safety incidents or uncertain remote state until resolved; record skipped slots and never claim unrun readiness checks passed.

Scoped readability revision (2026-09-08, ADR-029): owner requests stronger line breaks, paragraph separation and contextual emojis in existing private70/71/72. Permit only body formatting/editorial readability changes through the normal editor; retain facts, material caveats, source links, four images/credits/representatives, titles/IDs/URLs/dates and private visibility. Do not restore removed operational footers. No new publication, uploads, skin/global settings or broader runtime authority.

Scoped footer cleanup (2026-09-08, ADR-028): owner requests removal of the final source/check-date/non-experience boilerplate from the three current private articles70/71/72. Remove that paragraph and its now-orphaned separator only; preserve inline source links, image credits, substantive conditions, images, titles, representatives and private IDs/URLs/dates. Retain source/date/rights evidence internally. Future reader-facing drafts should not append this operational boilerplate by default; this is not a waiver of necessary attribution, monetization disclosures, material limitations or source checks. No other historical-post changes or wider publishing authority.

Scoped reader-v3 authority (2026-09-08, ADR-027): owner approves square-safe cover replacement and requests two additional images per existing article (four total including cover), younger conversational Korean with restrained slang/emojis, and removal of repetitive AI/illustration/reconstruction labels. Covers and explanatory graphics may be generated/edited and uploaded to private70/71/72; title/body may be revised accordingly. No fabricated first-person experience, documentary photos, official-looking forged material or removed provenance metadata. Keep source/date/rights provenance in the repository and factual captions where misunderstanding is material; no mandatory AI badge on obviously stylized drawings. Preserve private IDs/URLs and previous bytes, run fresh representation and saved-surface checks. No skin/settings change, new posts, public release, installation or runtime-publisher permission.

Scoped reader-v2 edit authority (2026-09-07, ADR-026): owner explicitly approved replacing the title/body of existing private posts70/71/72 with the three reader-v2 drafts and uploading their six generated images. Preserve post identities and private visibility. Use normal editor/file upload through the existing owner session, one post at a time, with pre-edit snapshot, exact representation/media checks and saved-state readback. No new creation, public/scheduled release, deletion, skin/settings change, other source-photo reuse or runtime-publisher permission is added. Stop on ambiguous upload/save or conflicting owner edits; do not recreate a post.

Scoped Top5 private-delivery authority (2026-09-07, ADR-025): owner requests the five current batch drafts privately saved on nedamma.tistory.com. Apply source/quality/representation gates independently to each; the request does not waive unresolved source-use or authorize public release, existing-post mutation, settings or runtime publishing. Use the existing normal-editor session, check duplicates and stop on uncertain save state. The earlier Gemini-only exception does not automatically apply.


Scoped assisted-delivery approval (2026-09-07, ADR-023): the owner approved one Gemini article's private/draft entry and actual editor verification on `nedamma.tistory.com`. This is a separate operator-assisted rehearsal, not a change to the offline runtime or automatic-publishing authority. Use only the owner's manually logged-in session after current-policy and target/duplicate checks. No public/scheduled post, existing-post modification, deletion, settings change, cookie extraction or login bypass. Stop on ambiguous save state; do not retry a possible creation. Record any transformed editor content separately from the immutable local review bundle and verify it before and after saving.

Owner-approved single-article exception (2026-09-07, ADR-022): for Gemini subject `855a746627c92ea90e94d1f65773eac8fc55f0dba28b345f32966e0dcc0902f3` only, actual screen-reader, detailed-print and Lighthouse certification are deferred rather than passed. Existing exact-build content/source/responsive/zoom/keyboard evidence may support independent final local-package review. No other article, content change, quality/rights/freshness threshold, review binding or external-write authority is covered. Record accepted debt alongside the package, outside immutable bundle bytes.

1. Contract and source evidence are complete enough for the input’s claim class.
2. Claim, policy, originality, accessibility, and render checks produce structured results.
3. The approval package is generated only after every required local gate passes.
4. Any future external action remains separately owner-approved, policy-rechecked, rate-limited, verifiable, and recoverable.

## Changes to these rules

Update this file when a repository-owned operating rule changes. Record the why and alternatives in `DECISIONS.md`; do not silently encode policy in a prompt or a tool invocation.
